# `src/registry/` (planned)

Holds the TypeScript extension surface:

- `NodeRegistry` (node type lookup)
- `NodeTypeDefinition` (schemas + runtime handler + capabilities)

Users should be able to define their own node types in TypeScript and register them.

