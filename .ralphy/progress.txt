# Ralphy Progress Log
# Iteration started: Mon Jan 19 14:42:31 GMT 2026

## 2026-01-19 14:42 - PRD Created

### Declarative Agent Pattern with Structured Outputs

Created PRD to fix agent→handler contract mismatch by:
1. Adding `defineAgent()` utility with Zod→JSON schema conversion
2. Creating shared schemas for agents and handlers
3. Wiring structured outputs through workflow
4. Migrating planner agent as first example

Key insight: ClaudeHarness ALREADY supports structured outputs,
we just need to wire Zod schemas through agent config.

Next step: /ralphy:tasks


## 2026-01-19 14:43 - Tasks Generated

### Validation: PASSED

Generated tasks.yaml with 4 phases, 9 tasks:
- Phase 1: Dependencies & Core Utility (3 tasks)
- Phase 2: Shared Schemas (2 tasks)
- Phase 3: Agent & Workflow Wiring (3 tasks)
- Phase 4: Verification (1 task)

Constitution check: 0 violations

Next step: /ralphy:run


## 2026-01-19 15:04 - Task 1.1 Complete

### Zod 4 Native JSON Schema Support Verified

**Discovery**: The original task specified using `zod-to-json-schema`, but that
library doesn't support Zod 4.x (returns empty schema object). Zod 4 has native
JSON Schema conversion via `z.toJSONSchema()`.

**Actions taken**:
1. Installed zod-to-json-schema to test - confirmed incompatible with zod 4.x
2. Verified Zod 4's native `z.toJSONSchema()` works correctly
3. Removed zod-to-json-schema (not needed with native support)
4. Updated tasks.yaml to reflect the correct approach

**Impact on remaining tasks**:
- Task 1.2 will use `z.toJSONSchema(schema)` instead of `zodToJsonSchema()`
- No additional dependency needed
- Simpler implementation

Tests: PASSED
Lint: PASSED

Next step: Task 1.2 - Create defineAgent utility


## 2026-01-19 15:14 - Tasks 1.2 & 1.3 Complete

### defineAgent Utility Created and Exported

**Task 1.2: Create defineAgent utility**

Created `packages/internal/core/src/api/define-agent.ts` with:
- `defineAgent<TOutput, TState>()` function accepting Zod schemas
- Uses Zod 4's native `z.toJSONSchema()` for conversion (no external lib)
- Returns `CompiledAgent` with both `zodSchema` and `jsonSchema`
- Supports dynamic prompts: `prompt: string | (ctx) => string`
- Supports `when` guards for conditional activation
- Supports `updates` field for direct state mapping

**Key design decisions**:
1. Custom `ZodSchema<T>` type for Zod 3/4 compatibility
2. JSON Schema cached at definition time (not per-call)
3. Type inference preserved through generic parameters

**Task 1.3: Export from @internal/core**

Updated `packages/internal/core/src/api/index.ts` to export:
- `defineAgent` function
- `AgentDefinition` type
- `CompiledAgent` type
- `JSONSchema` type

**Tests**: Created comprehensive test suite with 9 tests covering:
- Basic schema conversion
- Dynamic prompts
- When guards
- Agents without schemas
- Complex nested schemas
- Type inference

**Verification**:
- All 143 tests pass
- Typecheck passes (15 packages)
- Lint passes (14 packages)

Next step: Task 2.1 - Create plan.schema.ts with Zod schemas


## 2026-01-19 16:30 - Task 2.1 Complete

### Shared Zod Schemas Created

**Task 2.1: Create plan.schema.ts with Zod schemas**

Created `packages/prd-workflow/src/schemas/plan.schema.ts` with:
- `TaskStatusSchema` - enum for task status
- `AttemptRecordSchema` - record of task execution attempts
- `TaskSchema` - full task definition (matches Task interface)
- `MilestoneSchema` - milestone definition (matches Milestone interface)
- `PlanCreatedPayloadSchema` - structured output for planner agent

**Key design decisions**:
1. All schemas use `.describe()` for LLM-friendly documentation
2. Default values for status/attempt/passed ensure agents can return minimal data
3. Types exported via `z.infer<>` for single source of truth
4. Structure exactly matches existing interfaces in types.ts

**Tests**: Created comprehensive test suite with 12 tests covering:
- Schema validation for all types
- Default value application
- JSON Schema generation via `z.toJSONSchema()`
- Description preservation in generated JSON Schema

**Verification**:
- All 54 tests pass in prd-workflow (12 new + 42 existing)
- Lint passes

Next step: Task 2.2 - Create schemas index and update types.ts


## 2026-01-19 17:00 - Task 2.2 Complete

### Schemas Index and types.ts Updated

**Task 2.2: Create schemas index and update types.ts**

Created `packages/prd-workflow/src/schemas/index.ts` barrel export with:
- All schemas: `TaskStatusSchema`, `AttemptRecordSchema`, `TaskSchema`, `MilestoneSchema`, `PlanCreatedPayloadSchema`
- All inferred types: `TaskStatus`, `AttemptRecord`, `Task`, `Milestone`, `PlanCreatedPayload`

Updated `packages/prd-workflow/src/types.ts` to:
- Re-export all types from `./schemas/index.js` (single source of truth)
- Re-export all schemas for direct use
- Removed duplicate `PlanCreatedPayload` interface definition
- Import locally aliased types for use within the file (`Task`, `Milestone`, `PlanCreatedPayload`)

**Key design decisions**:
1. Barrel export pattern for clean imports
2. Types re-exported from schemas are the canonical source
3. Local type aliases needed because `export type` doesn't make types available within the same file
4. Backward compatibility maintained - existing consumers continue to work

**Tests**: Created two comprehensive test files:
- `schemas/index.test.ts` - 12 tests for schema exports and type inference
- `types.test.ts` - 12 tests for re-exports and local types

**Also fixed**:
- Pre-existing TypeScript strict null check errors in `plan.schema.test.ts` (lines 159-160)

**Verification**:
- All 80 tests pass in prd-workflow
- Typecheck passes (15 packages)
- Lint passes (14 packages)

Next step: Task 3.1 - Create planner.agent.ts


## 2026-01-19 17:30 - Task 3.1 Complete

### Planner Agent Created with defineAgent

**Task 3.1: Create planner.agent.ts**

Created `packages/prd-workflow/src/agents/planner.agent.ts` with:
- `plannerAgent` using `defineAgent()` from @internal/core
- `outputSchema: PlanCreatedPayloadSchema` for structured Claude output
- Dynamic prompt function that injects PRD from `ctx.state.planning.prd`
- `activateOn: ["workflow:start"]` for initial trigger
- `emits: ["plan:created"]` signal declaration
- `when` guard ensuring activation only when `planning.phase === "idle"`

**Key design decisions**:
1. Dynamic prompt `(ctx) => string` allows runtime PRD injection
2. When guard prevents duplicate plan generation if workflow restarts
3. Agent is generic typed for both output schema and workflow state
4. Created `agents/index.ts` barrel export for clean imports

**Tests**: Created comprehensive test suite with 13 tests covering:
- Agent structure (tags, reactive markers, schemas)
- Activation configuration (signals, emits)
- JSON Schema output validation
- Dynamic prompt behavior
- When guard logic

**Verification**:
- All 96 tests pass in prd-workflow (13 new + 83 existing)
- Typecheck passes
- Lint passes

Next step: Task 3.2 - Update create-workflow.ts to wire schemas


## 2026-01-19 18:15 - Task 3.2 Complete

### Schema Wiring in create-workflow.ts

**Task 3.2: Update create-workflow.ts to wire schemas**

Modified `packages/internal/core/src/api/create-workflow.ts` to:
1. Pass `outputSchema` to harness when agent has `jsonSchema` (from defineAgent)
2. Extract `structuredOutput` from harness response
3. Use `structuredOutput` directly as signal payload when available
4. Fall back to `{ agent, output }` wrapper for text-only agents

**Key changes**:
1. Added `AgentExecutionResult` interface to carry both raw output and structuredOutput
2. `executeAgent` now accepts optional `jsonSchema` parameter
3. Includes `outputSchema` in harness input when schema is provided
4. Signal emission uses structuredOutput directly as payload (type-safe handlers!)
5. `updates` field also prefers structuredOutput for state updates

**Implementation details**:
- Runtime check for `jsonSchema` property on agent (may exist from defineAgent)
- Backward compatible: agents without schemas continue to work
- Mixed workflows supported: defineAgent and factory agent() can coexist

**Tests**: Created comprehensive test suite with 8 tests covering:
- JSON Schema passing to harness
- structuredOutput extraction from harness response
- Signal payload uses structuredOutput when available
- Fallback to wrapper when no structured output
- updates field with structuredOutput
- state change signal with structuredOutput value
- Backward compatibility with factory agents
- Mixed defineAgent and factory agents in same workflow

**Verification**:
- All tests pass (including 8 new schema wiring tests)
- Typecheck passes (15 packages)
- Lint passes (14 packages)

Next step: Task 3.3 - Update CLI to use planner agent


## 2026-01-19 19:00 - Task 3.3 Complete

### CLI Updated to Use plannerAgent

**Task 3.3: Update CLI to use planner agent**

Modified `packages/prd-workflow/src/cli.ts` to:
1. Import `plannerAgent` from `./agents/index.js` (dynamic import)
2. Removed inline agent definition using `createPRDWorkflow` factory
3. Pass `plannerAgent` directly to `runPRDWorkflow`
4. Updated both live and record modes to use `{ planner: plannerAgent }`

**Key changes**:
1. Dynamic import of `plannerAgent` alongside workflow functions
2. Removed `createPRDWorkflow` import (no longer needed for agent factory)
3. Comment added explaining that agent's dynamic prompt reads PRD from state
4. Both record and live modes now use the declarative agent

**Also fixed**:
- Updated `ReactiveAgentConfig.prompt` type in create-workflow.ts to support
  dynamic prompts: `string | ((ctx: ActivationContext<TState>) => string)`
- Added prompt resolution logic in `executeAgent` to call function if needed

**Tests**: Created CLI test suite with 13 tests covering:
- plannerAgent export availability and structure
- JSON Schema presence for structured output
- Activation signals and emission configuration
- CLI source file verification (imports, no inline agent)
- Agent compatibility with workflow config
- Dynamic prompt and when guard presence

**Verification**:
- All 109 tests pass in prd-workflow (13 new CLI tests)
- Typecheck passes (15 packages)
- Lint passes (14 packages)

Next step: Task 4.1 - Run quality gates and live test
