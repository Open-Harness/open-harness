flow:
  name: phases-tasks-demo
  description: Demonstrates phases and tasks pattern
  nodePacks: [core]

nodes:
  - id: plan
    type: constant
    input:
      value:
        text: "Define milestones and architecture"

  - id: build
    type: constant
    input:
      value:
        text: "Implementation complete - ship it!"

edges:
  - from: plan
    to: build
