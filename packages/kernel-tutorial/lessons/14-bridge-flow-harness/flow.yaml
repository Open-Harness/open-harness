flow:
  name: bridge-flow-harness
  nodePacks: [core]

nodes:
  - id: facts
    type: constant
    input:
      value: "Flow inside harness"

  - id: say
    type: echo
    input:
      text: "{{facts.value}}"

edges:
  - from: facts
    to: say
