# Lesson 14: Flow ↔ Harness Bridge

## Goal
Show how flows execute *inside* the harness runtime.

## Key Concepts
- Flow execution uses `phase` + `task` helpers from the harness.
- Harness events surround flow node runs.

## Run
```bash
bun run lesson:14
```
